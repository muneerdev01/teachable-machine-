# teachable-machine-
A web-based AI system that lets users create and train their own image classification models. Users can define custom labels, upload images, train a model with one click, and test predictions in real time. Built using Streamlit for UI and FastAPI for backend with a pre-trained MobileNet feature extractor.
